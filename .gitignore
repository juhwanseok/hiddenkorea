# secrets
.env
*.key

# python
__pycache__/
*.pyc
.venv/
venv/

# node (W4~)
node_modules/
.next/

# data — 원천 데이터 커밋 금지 (DATA_GUIDELINES). 단, d2_spots/region_map 등 소용량 참조표는 허용
data/raw/
*.parquet
*.db
*.db-journal
*.log
*.png
# 파생 산출물(파이프라인으로 재생성) — 임베딩·ML모델
data/embeddings.npz
data/congestion_gap_model.txt
data/congestion_gap_meta.json

# 단, 배포(Railway)에 필요한 산출물은 예외로 추적
!data/hiddenkorea.db
!data/embeddings.npz
!data/congestion_gap_model.txt
!data/congestion_gap_meta.json

# misc
.DS_Store
Thumbs.db
