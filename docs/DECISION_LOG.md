# DECISION_LOG — 숨은한국

> 형식: #번호 | 날짜 | 결정 | 근거 | 대안(기각 이유). 뒤집을 때는 새 항목으로 추가 (기존 항목 수정 금지).

| # | 날짜 | 결정 | 근거 | 기각된 대안 |
|---|---|---|---|---|
| 1 | 07-08 | 지정과제 2(오버투어리즘) 선택, 서비스명 「숨은한국」 | 가중 매트릭스 8.35점 1위. 데이터 실증 결과 집중률 예측 API·연관 관광지 API 실존 확인, AI 3계층이 각각 독립 성립(리스크 헤지) | 과제9 축제예보(정답 데이터 불일치 구조 리스크), 과제6 반려동물(독창성 상한) |
| 2 | 07-08 | 혼잡도는 "실시간"이 아닌 "예측 기반"으로 포지셔닝 | 지방 실시간 무료 소스 부재. 예측은 D2 API+자체 ML로 전국 커버 가능. "여행 계획 단계 의사결정" 프레임이 과제 부합성도 높음 | 실시간 서비스 (서울 한정이라 기각) |
| 3 | 07-08 | 프론트 = Next.js+shadcn (Streamlit 기각) | "완성 서비스" 인상 = 구현성 30점. Streamlit은 데모 도구 인상. 프론트 약점은 AI 코딩으로 보완 | Streamlit(빠르지만 심사 인상 손해), React SPA(라우팅·SEO 수공) |
| 4 | 07-08 | DB = Supabase Postgres+pgvector 단일 | 벡터+관계형 단일 운영, 무료 티어, 백업 자동 | FAISS+SQLite(운영 이원화), Pinecone(비용) |
| 5 | 07-08 | 혼잡 근사 모델 = LightGBM (딥러닝 기각) | 소규모 표형식 데이터, 해석가능성(발표 소재), 학습 수분 | LSTM/Transformer(데이터 규모 대비 과분, 설명 어려움) |
| 6 | 07-08 | 임베딩 파인튜닝 안 함 | 골드셋 규모(30쌍)로는 튜닝 불가, e5 사전학습으로 충분 | KoSimCSE 파인튜닝(시간·데이터 부족) |
| 7 | 07-08 | LLM은 단발 structured output 2종만 (Agent 기각) | 지연·실패 표면적 최소화 = 구동 안정성. 사실은 DB 주입으로 환각 차단 | LLM Agent(도구호출 실패 모드 다수, 과제1스러워짐) |
| 8 | 07-08 | 로그인 없음 | 심사위원 마찰 제거 + 개인정보 이슈 원천 차단 | 소셜 로그인(가치 대비 리스크) |
| 9 | 07-11 | **R1 게이트 통과 — 하이브리드 설계 확정**. 집중률 API가 1차 소스, 자체 ML은 커버리지 갭 보완 | 전국 실측: 고유 관광지 **7,445곳**, 예측 지평 **30일**, `areaCd`(행정표준코드 시도2)+`signguCd`(시군구5) 조회, 필드 `tAtsNm`/`cnctrRate`/`baseYmd`. 500 기준 대폭 상회 | (R1 발동 시나리오였던 ML 전면 대체는 불필요) |
| 10 | 07-11 | 커버리지 갭 2곳(코드 12 '전남광주통합특별시', 36110 세종)은 자체 ML/시군구 방문자수로 근사 | 집중률 API가 두 지역 0건 반환. 세종은 시군구 미분할 특성 | 해당 지역 서비스 제외(사용자 경험 훼손이라 기각) |
| 11 | 07-11 | POI 마스터 = TourAPI KorService2 areaBasedList2 전량(50,674건) 로컬 SQLite 적재. `lDongRegnCd+lDongSignguCd`가 집중률 signguCd 조인키 | EDA: 이미지 85.5%·좌표 100%·**집중률↔TourAPI 제목매칭 85.2%(6,343/7,445)**. 신형 API라 cat1~3 비고 lclsSystm1~3 사용 | 구형 KorService1(cat 체계, lDong 없음 → 조인 불가) |
| 12 | 07-11 | overview 임베딩은 W3에서 **매칭 후보 풀(관광지+문화시설 ~15k)만 선별 수집**, v0는 제목+분류+주소 임베딩으로 시작 | detailCommon2가 POI당 1콜 → 5만 콜=50일(개발쿼터). 운영계정 신청 또는 선별 필요 | 전량 overview 수집(쿼터 불가) |
| 13 | 07-11 | 임베딩 모델 = **paraphrase-multilingual-MiniLM-L12-v2**(384d, fastembed/ONNX/CPU) | e5-small은 현 fastembed 미지원. MiniLM은 한국어 지원·경량·torch 불필요 | e5-large(1024d, ~2GB 과중), sentence-transformers+torch(설치 무거움) |
| 14 | 07-11 | 매칭 후보생성 = **lclsSystm2(공식 중분류) 필터 우선** + 임베딩/거리/혼잡 랭킹. 임베딩 단독 폐기 | 짧은 제목+주소 임베딩은 변별력 부족(경복궁 유사상위에 올리브영·약국). lclsSystm은 공식·정확(경복궁 HS01=고궁류) | 임베딩 단독(노이즈 심함), overview 임베딩(쿼터 불가) |
| 15 | 07-11 | 매칭 평가지표 = **분류정합률 + 혼잡저감 + 정성표**로 재정의. 유사도 nDCG 폐기 | 핵심 발견: 유사도 골드셋은 '유명하지만 붐비는' 명소를 정답 요구 → 오버투어리즘 분산 목적과 상충(붐비는 대안 제외가 옳음). 결과: 분류정합 100%, 평균혼잡 31.8 | nDCG@3(목적과 불일치, 붐비는 정답 강요) — 발표에서 '벤치마크의 함정' 스토리로 활용 |
